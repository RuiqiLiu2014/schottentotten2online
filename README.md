One person runs the host code, main class under Host.java
One person runs the client code. Change "localhost" to the host's IP address (in quotations).
Made to run in intelliJ, emojis may not load in other IDEs. Will make a non-emoji mode soon.
